version: '3.9'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - UVICORN_PORT=8000
    volumes:
      - .:/app
    command: uvicorn src.backend.app:app --host 0.0.0.0 --port 8000 --reload
